spring.application.name=altinhedef

# Database Connection
spring.datasource.url=jdbc:postgresql://localhost:5432/altinhedef_db
spring.datasource.username=altinhedef_user
spring.datasource.password=qazaltinwsx

# JDBC and Database Platform
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Hibernate -- Change on prod to update, To validate use validate
spring.jpa.hibernate.ddl-auto=update

# Show SQL Logs on console
spring.jpa.show-sql=true

# Logging Level
logging.level.org.springframework.security=DEBUG

# JWT Config
jwt.refresh-token.expiration-in-ms=604800000